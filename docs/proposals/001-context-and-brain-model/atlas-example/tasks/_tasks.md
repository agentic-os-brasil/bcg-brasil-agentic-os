# ✅ Tasks — index

> Mother file (MOC) for `atlas/tasks/`. The backlog (+ any external task-tool sync rules).
> Up: [Proposal 001 — Context and Brain Model](../../001-context-and-brain-model.md), §4.

## Members
- `backlog.md` — the master task list and priorities.
- `<tool>-sync.md` — sync rules if the backlog is mirrored into an external task tool.
