interface:
  display_name: "Investigate"
  short_description: "Find the root cause of a wrong or surprising result"
  default_prompt: "Use $investigate to find why this output is wrong, with the evidence that establishes the cause."
