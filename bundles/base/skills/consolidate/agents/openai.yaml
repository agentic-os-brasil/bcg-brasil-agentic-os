interface:
  display_name: "Consolidate"
  short_description: "Propose ranked cleanup from supplied records without editing"
  default_prompt: "Use $consolidate to identify the highest-value cleanup proposals in these records."
