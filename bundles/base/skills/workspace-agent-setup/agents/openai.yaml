interface:
  display_name: "Alias Legado do Case Agent"
  short_description: "Redireciona o nome aposentado workspace-agent-setup"
  default_prompt: "Use $case-agent-setup; este alias legado apenas redireciona chamadas existentes durante a migração."
