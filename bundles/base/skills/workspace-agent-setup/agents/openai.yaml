interface:
  display_name: "Legacy Case Agent Setup Alias"
  short_description: "Migrate the retired workspace-agent setup name"
  default_prompt: "Use $case-agent-setup; this legacy alias only redirects existing callers during migration."
