interface:
  display_name: "Decision Log Entry"
  short_description: "Register a structural case decision in the active case decision log"
  default_prompt: "Use $decision-log-entry to register this decision in the active case log."
