{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:bcg-brasil-agentic-os:schema:interaction-profile:v1",
  "title": "BCGOS Interaction Profile",
  "type": "object",
  "required": ["schema_version", "profile"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {"const": 1},
    "profile": {"enum": ["standard", "advanced", "power"]}
  }
}
