# Secrets and local configuration
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# BCG Agentic OS local state
.bcgos/local/
.bcgos/cache/
.bcgos/logs/
.bcgos/secrets/
bcgos-data/

# Maestro dreaming marker — written by SessionStop hook, consumed by SessionStart
# hook. Local ephemeral signal only; must never be committed.
**/data/memory/.dream-requested
.dream-requested

# Build artifacts
bin/
dist/
coverage/
cover.out
*.test

# Local release exports are published as immutable GitHub Release assets.
# Keep the directory visible in the source tree without committing binaries.
/releases/*
!/releases/.gitkeep

# Editors and operating systems
.DS_Store
Thumbs.db
.idea/
.vscode/

# Temporary files
*.tmp
*.bak
*.swp
